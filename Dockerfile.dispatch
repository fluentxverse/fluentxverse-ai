# Use official Bun image
FROM oven/bun:1.3-alpine

WORKDIR /app

# Copy package files
COPY package.json bun.lock* ./

# Install dependencies
RUN bun install --frozen-lockfile

# Copy source files
COPY . .

# Default command - run the scheduler
CMD ["bun", "run", "scheduler:start"]
